[build]
builder = "dockerfile"
dockerfilePath = "../Dockerfile"
watchPatterns = ["api/**", "skills/**", "Dockerfile"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
